{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch --env-file=.env server.js"
  },
  "author": "Jay",
  "license": "ISC",
  "description": "Behind your todo",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  }
}
